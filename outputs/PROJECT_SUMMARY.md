# Blockchain-Based Healthcare Record System

The complete project has been scaffolded in this workspace with:

- Backend API in `backend/`
- Solidity smart contracts in `blockchain/`
- React frontend in `frontend/`
- Final-year project documentation in `docs/`

Start with the root `README.md`, then run each package after installing dependencies.

